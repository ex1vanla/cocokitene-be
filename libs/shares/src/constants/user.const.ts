export enum UserRole {
  // user'role
  USER_ADMIN = 'user_admin',
  USER_SUPER_ADMIN = 'user_super_admin',
  USER_NORMALLY = 'user_normally',
  USER_SHAREHOLDER = 'user_shareholder',
}

export enum UserStatusEnum {
  UNVERIFIED = 'unverified',
  VERIFIED = 'verified',
  ACTIVE = 'active',
}
